#include<stdio.h>
void main()
{
    int n,i;
    int max;
    printf("Enter the number of element in array:");
    scanf("%d",&n);
    int arr[n];
    printf("Enter the element for array");
    for(int i=0;i<n;i++)
    {
        printf("\n Enter the element arr[%d]:",i);
        scanf("%d",&arr[i]);
    }
    max=arr[0];
    for(int i=1;i<n;i++)
    {
        if(arr[i]>max)
        {
            max=arr[i];
        }
    }
    printf("The largest element in array  is %d\n",max);
}
---------------------------------------------------------------------
Output
Enter the number of element in array:4
Enter the element for array
 Enter the element arr[0]:15
 Enter the element arr[1]:2
 Enter the element arr[2]:9
 Enter the element arr[3]:8
The largest element in array  is 15
----------------------------------------------------------------------
